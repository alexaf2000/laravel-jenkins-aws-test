pipeline {
  agent any
  stages {
    stage('Print something') {
      steps {
        echo 'All is correct?'
      }
    }

    stage('IP CONFIG') {
      steps {
        sh 'cmd && ipconfig'
      }
    }

  }
}